<script type="text/javascript" src="@Url.Content(@"~/Areas/Schedule/Scripts/templateList.js")"></script>
<script id="templateListTemplate" type="text/html">
<fieldset class="itemList">
    <legend>Templates</legend>
    <div id="templateList">
        {{each GetCourses(model)}}
            <h3 courseName="${$index}"><a href="#">${$index}</a></h3>
            <div>
                <ul>
                {{each $value.templates}}
                    <li>@Html.TemplateActionLink("${name}", "Details", new {id = "${id}"})</li>
                {{/each}}
                </ul>
            </div>
        {{/each}}
    </div>
</fieldset>
</script>
